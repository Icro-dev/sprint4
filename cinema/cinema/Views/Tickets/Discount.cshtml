@using cinema.Models
@model cinema.Models.Ticket

@{
    ViewData["Title"] = "Create";
    Layout = "~/Views/Shared/_Layout.cshtml";
    int quantity = ViewBag.quantity;
}

<h2 class="mt-5">Kortingen</h2>
<hr/>

<form method="get" asp-action="Reservation">
    <div asp-validation-summary="ModelOnly" class="text-danger"></div>
    <input type="hidden" name="quantity" value="@quantity">
    <input type="hidden" name="showId" value="@ViewBag.show">
    <input type="hidden" name="arrangement" value="@ViewBag.Arrangement">
    <div class="mb-4">
        <label class="form-check-label">
            Kinder korting (- €1.50)
            <select name="childDiscount" id="childDiscount">
                @for (int discount = 0; discount < @quantity + 1; discount++)
                {
                    <option value="@discount">@discount</option>
                }
            </select>
        </label>
    </div>
    <div class="mb-4">
        <label class="form-check-label">
            Studenten korting (- €1.50)
            <select name="studentDiscount" id="studentDiscount">
                @for (int discount = 0; discount < @quantity + 1; discount++)
                {
                    <option value="@discount">@discount</option>
                }
            </select>
        </label>
    </div>
    <div class="mb-4">
        <label class="form-check-label">

            Senioren korting (- €1.50)
            <select name="seniorDiscount" id="seniorDiscount">
                @for (int discount = 0; discount < @quantity + 1; discount++)
                {
                    <option value="@discount">@discount</option>
                }
            </select>
        </label>
    </div>
    <h3 class="mt-5">Extra opties</h3>
    <hr>
    <div class="mb-4">
        <label class="form-check-label">
            Popcorn (+ €2.5)
            <select name="popcorn" id="popcorn">
                @for (int discount = 0; discount < @quantity + 1; discount++)
                {
                    <option value="@discount">@discount</option>
                }
            </select>
        </label>
    </div>
    <div class="form-group">
        <input type="submit" value="Volgende" class="btn btn-primary"/>
    </div>
</form>

@section Scripts {
    @{ await Html.RenderPartialAsync("_ValidationScriptsPartial"); }
}